**优化后的模板：**

每条拆成这个结构更好——

```
@incremental-implementation

## 目标
一句话说清楚最终用户可见的变化。

## 当前状态
- 后端: `文件路径` → `函数名()` (line XX)
- 前端: `文件路径` → `组件名` (line XX)
- 样式: `文件路径` → `.class名` (line XX)
- 数据库: `migrations/0XX_xxx.sql`

## 约束
- 不改已有 API 返回格式（只增字段）
- 不改已有 CSS class 属性（只追加新 class）
- 不改 store 已有 state 命名
- 不引入新依赖
- 不删任何现有代码
- 每步改完能单独运行不报错
- 新增/引用的 CSS 选择器,必须 grep JSX 确认真实渲染,贴出命中行号;零命中即停手报告,不得凭 CSS 中有定义就认为可用
- 与当前任务无关的改动(测试修复、迁移遗留、顺手重构)必须单独 commit,严禁与任务改动混入同一 commit

## 步骤（按依赖顺序，每步独立 commit）

### 步骤 1: [层级] 简述
**文件**: `path/to/file`
**位置**: `函数名()` / line N~M
**当前代码**:
```

