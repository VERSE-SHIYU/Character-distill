# 仅开发/调试时需要（Gradio 旧入口已废弃）
gradio>=5.0.0
pytest
pytest-asyncio
# MCP SDK —— mcp_server 协议层测试依赖（test_rag_unusable 走 mcp_server 降级路径）。
# 生产 app 不 import mcp（core/web/storage/adapters 无引用），故不入 requirements.txt
mcp==1.26.0
