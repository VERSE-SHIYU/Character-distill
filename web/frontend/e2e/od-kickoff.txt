开始执行。按 brief 的顺序推进，产出到项目 artifacts：

第一步：先定义完整设计系统 —— 字体 scale、间距 scale、圆角一致性锁、阴影/elevation、6 套主题的亮/暗 token 映射、组件规范（按钮/卡片/输入框/气泡/列表/TabBar）。

第二步：把设计系统应用到关键页面原型 —— 登录、角色市场（网格）、角色卡片、沉浸式对话（聊天气泡+输入栏+语音按钮）、群聊、私信、蒸馏任务进度条、主题设置、个人中心。

视觉方向：给这个 AI 角色扮演产品一个有性格的设计语言，偏向「对话/剧场」的温暖克制气质，有情感温度但不油腻，可落地。中文界面，移动优先（390px），桌面双栏延伸。

每完成一块就落地成一个 HTML 文件到项目目录，让我能看到可交互的成品。
