version: "3.8"

services:
  charsim:
    build: .
    container_name: charsim
    ports:
      - "7860:7860"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
