---
description: 全局执行规则（每个 Step 开头默认生效）
alwaysApply: true
---

## 全局规则（每个 Step 开头默认生效）

你是执行者，不是顾问。以下规则贯穿所有 Step：

1. 不准问问题。所有决策已在指令中写死。
2. 不准写 spec/文档/方案。直接写可运行的代码。
3. 每个文件写完必须可独立运行或 import 不报错。
4. 中文注释，类型注解，docstring 三件套。
5. 错误处理：API 调用失败、文件读取失败、JSON 解析失败——全部 try-except 并 print 友好中文提示，不准静默吞错。
6. 写完一个 Step 后，给我一条验证命令（如 python xxx.py），我跑通了才进下一步。
