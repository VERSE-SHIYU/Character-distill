# ═══════════════════════════════════════════════════════════════════════════
# 人写的约束（下界）。**锁文件是 requirements.txt，由 uv 生成，CI/Docker 按锁装。**
# 本文件只管「至少要多新」，不管「这次装到了哪一版」—— 后者是锁文件的职责。
#
# 改完本文件必须重锁（仓库根目录）：
#   uv pip compile requirements.in -o requirements.txt --universal --python-version 3.12
#
# 不要手改 requirements.txt：它是产物，手改会被下次重锁冲掉，且 CI 会红
# （build.yml 的 "Verify installed versions match the lock" 步骤当场核对）。
# ═══════════════════════════════════════════════════════════════════════════
openai>=1.30.0
pydantic>=2.0
chromadb>=0.5.0
pyyaml>=6.0
python-dotenv>=1.0.0
fastapi>=0.115.0
slowapi>=0.1.9
uvicorn>=0.30.0
aiosqlite>=0.20.0
edge-tts>=6.1.0
pymupdf>=1.28.2
python-docx>=1.1.0
aiofiles>=24.1.0
mutagen>=1.47.0
httpx>=0.27.0
pycryptodome>=3.20.0
websockets>=12.0
mem0ai>=2.0.0
PyJWT>=2.8.0
cryptography>=42.0.0
pwdlib[argon2]>=0.2.0
python-multipart>=0.0.9
resend>=2.30.1,<3.0.0
psutil>=6.0.0
asyncpg>=0.29
XdbSearchIP>=1.0.2
jieba>=0.42.1
opentelemetry-api>=1.41.1,<1.42.0
opentelemetry-sdk>=1.41.1,<1.42.0
opentelemetry-exporter-otlp-proto-http>=1.41.1,<1.42.0
